# Agent Boot

import { Meta } from '@storybook/blocks';

<Meta title="Docs/Agent Boot" />

Welcome. This is the instant-on boot page for an agent.

Quick facts
- Purpose: QuizMentor — quiz + gamification with mock-first dev.
- Canonical docs:
  - DevLog: /docs/status/DEVLOG.md
  - Epics: /docs/roadmap/EPICS.md
  - System Status: /docs/SYSTEM_STATUS.md
- How to run:
  - Dev (web): npm run start
  - Storybook: npm run storybook
  - Tests: npm run test (unit/integration), npm run test:e2e (Playwright)
  - Docs update: npm run docs:updates

Docs status (optional)
- See LastUpdated pills across Storybook or open Repo Docs Browser.

Architecture sketch
- Frontend: Expo/React Native + Web, TanStack Query, Zustand, Supabase client, MSW, WS mocks.
- Backend (stubs): Express routes with zod validation and Redis mock; MSW simulates server behavior.
- Testing: Jest + RTL, Playwright, Storybook Test Runner, MSW.

Start here tasks
- Review Docs/Epics for roadmap.
- Run npm run docs:updates and open Docs/Dev Log to confirm latest entry.
- Run npm run test and npm run test:e2e to validate local setup.

Known gaps
- Ensure all platform-specific RN/Expo mocks are green in Jest.
- Verify MSW defaults and scenarios are set as expected.

