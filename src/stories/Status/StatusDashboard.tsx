import React from "react";

const StatusDashboard: React.FC = () => (
  <div style={{ padding: 16 }}>
    <h3>Status Dashboard</h3>
    <p>Artifacts will appear if available (coverage, Playwright report).</p>
  </div>
);

export default StatusDashboard;
