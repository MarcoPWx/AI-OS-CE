/* Removed story. Consolidated into a single Epic Manager page. */
// Minimal default export to satisfy Storybook indexer while keeping this story hidden
const removedEpicsEditorMeta = {
  title: 'Labs/Epics/Epics Editor (removed)',
  parameters: { docs: { disable: true } },
  tags: ['hidden'],
};
export default removedEpicsEditorMeta;
