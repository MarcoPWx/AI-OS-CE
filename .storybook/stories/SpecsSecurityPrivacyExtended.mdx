# Security & Privacy — Extended Model

import { Meta } from '@storybook/blocks';
import { InlineTOC } from '../components/InlineTOC';
import { BackToTop } from '../components/BackToTop';

<Meta title="Specs/Security/Security & Privacy — Extended Model" />
<a id="top"></a>

### Table of Contents

- Authentication (#auth)
- Authorization (#authz)
- Data Protection (#data)
- Secrets (#secrets)
- Privacy (#privacy)
- Observability Hygiene (#obs)

<InlineTOC
  items={[
    { id: 'auth', label: 'Authentication' },
    { id: 'authz', label: 'Authorization' },
    { id: 'data', label: 'Data Protection' },
    { id: 'secrets', label: 'Secrets' },
    { id: 'privacy', label: 'Privacy' },
    { id: 'obs', label: 'Observability Hygiene' },
  ]}
/>

Expanded guidance. Items not in this repo are labeled concept.

## Authentication (in-repo plan)

<a id="auth"></a>- Supabase OAuth (GitHub) + email/password - Short-lived access tokens; refresh
rotation

## Authorization (concept)

<a id="authz"></a>- Role-based controls on API routes - Organization scoping for multi-tenant data

## Data Protection

<a id="data"></a>- PII classification; redact in client logs (in-repo guidance) - HTTPS everywhere
(deployment policy)

## Secrets

<a id="secrets"></a>- Never expose secrets in stories or logs - Use environment variables and secret
managers (deployment policy)

## Privacy

<a id="privacy"></a>- Data export/delete endpoints (stubs): /api/users/export, DELETE /api/users/me

## Observability Hygiene

<a id="obs"></a>

<BackToTop />- No plaintext tokens; include requestId; sample sizes for logs
