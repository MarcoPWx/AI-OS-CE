# Observability Stack

import { Meta } from '@storybook/blocks';
import { LastUpdated } from '../components/LastUpdated';
import { InlineTOC } from '../components/InlineTOC';
import { BackToTop } from '../components/BackToTop';

<Meta
  title="Observability/Observability Stack"
  parameters={{
    helpDocs: [
      { href: '?path=/story/specs-service-catalog--page#flows', title: 'Service Catalog' },
      { href: '?path=/story/overview-architecture--page#layered-view', title: 'Architecture' },
    ],
  }}
/>
<a id="top"></a>

What exists today and how we observe it, plus planned extensions.

### Table of Contents

- Today (in-repo) (#today)
- Planned (concept) (#planned)
- Guidance (#guidance)

<InlineTOC
  items={[
    { id: 'today', label: 'Today' },
    { id: 'planned', label: 'Planned' },
    { id: 'guidance', label: 'Guidance' },
  ]}
/>

## Today (in-repo)

<a id="today"></a>- Status JSON: docs/status/SYSTEM_STATUS_STATE.json consumed by status stories -
Storybook Test Runner: play() assertions; can add a11y scans - API Demos: response shapes, headers
(ETag, Retry-After) visible in stories - Network Playground: live request timeline with
status/duration

## Planned (concept)

<a id="planned"></a>- Structured client events: quiz_start, answer_submitted, quiz_completed,
network_error - Event pipeline: /api/analytics/event → aggregation store - Tracing: frontend spans
(navigation, requests) → backend spans (routes, DB) - Metrics: rate-limit counters, cache hit rates,
request latency percentiles

## Guidance

<a id="guidance"></a>

<BackToTop />
<LastUpdated note="Observability" />- Use Service Catalog to find endpoints and expected
headers/status codes - Add play() checks to enforce critical invariants
